\begin{table}[h]
\caption{\textbf{Extended Experimental Settings.}}
\centering
\label{tab:experimental-settings}

\begin{tabular}{@{}p{0.27\linewidth}p{0.67\linewidth}@{}}
\toprule
\textbf{Setting} & \textbf{Value used in the reported experiments} \\
\midrule
Seed prefixes & \MinProb: The $=[785]$, A $=[32]$, and In $=[641]$. \MaxSurprisal: A $=[32]$. Total length includes the seed. \\

One-byte ablation & Scan the vocabulary once; retain $v$ exactly when $[v]$
decodes to one ASCII code point with value 0--127 and that character
retokenizes to $[v]$. Qwen provides exactly 128 such IDs. \\

Model scores & Pretrained model without weight quantization, loaded with the
framework's automatic dtype. Logits are converted to float32 for ranking and
\texttt{logsoftmax}. Arithmetic-coder frequency quantization happens only
after generation and cannot affect the selected tokens. \\

Context & KV caching is enabled. At 1,000 context tokens, retain the latest
100 and rebuild the cache. Byte measurement and round-trip validation still
use the complete generated prefix. \\

Length and cost & \MinProb: 10,000 tokens for each of three seeds.
\MaxSurprisal: 1,024 tokens for one seed because it performs 151,643
context-sensitive decodes per step. \\
\bottomrule
\end{tabular}
\end{table}