# Exploratory experiments

This directory holds version-controlled experiments that are not tied to a paper.
Keep generated results under `artifacts/` and reusable implementations in `src/`.
Run commands from the repository root.

The encoding sweep is available at `research/exploratory/model_encoding_sweep.sh`.
Paper-specific commands, configurations, and analyses live with their paper under
`research/papers/`.
